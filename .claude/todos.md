# Todos

- [x] Remove the title text after the icon in the header navbar — keep just the icon with a tooltip showing the title
- [x] In /albums, move actions (edit / share / delete) to a new bar below the title and description
- [x] In /timeline, add sort toggle (ascending/descending) to the header
- [x] In /albums, move "sort by" selector to appear after the "type" filter in the header
- [x] In /persons, add sort by selector to the header: by photos count, by name — with toggle to reverse order
- [x] In /albums, add infinite scroll with pagination (like /persons)
- [ ] Explore caption translation (multi-language captions)
- [x] Person share links: same behavior as album sharing (temporary shared links)
- [x] In /albums, remove album page title; in /persons remove person page title
- [x] In small view: /albums, /persons, /timeline — add bottom bar with filters/sort (like gallery)
- [x] In /stats, add tooltip to tabs
- [x] In culling similar groups tab, move similarity slider inside tab content; keep only help toggle button in the tab header row
- [x] In culling, ensure padding between tabs and their content
